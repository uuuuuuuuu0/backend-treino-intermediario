model User {
    id         String   @id @default(uuid())
    name       String
    cpf        Int      @unique
    email      String   @unique
    password   String
    created_at DateTime @default(now())
    updated_at DateTime @updatedAt

    @@map("users")
}
